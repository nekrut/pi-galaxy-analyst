node_modules/

# Production Vite build output for the web shell
web/dist/

# macOS Finder metadata
.DS_Store

# Test-generated notebook files
*-notebook.md
activity.jsonl
session.jsonl

# Agent worktrees
.worktrees/

# Local agent state, except shared skills which are versioned with the repo
.claude/*
!.claude/skills/

# Eval run artifacts (Phase 5+; not committed)
evals/results/

# Eval credentials (TACC proxy etc; per-machine)
evals/.env
